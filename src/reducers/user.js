const INITIAL_STATE = {
  name: '',
  gravatar: '',
};

const user = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  case '':
    return state;
  default:
    return state;
  }
};

export default user;
